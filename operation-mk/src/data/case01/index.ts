/**
 * CASE MK-01 story data.
 *
 * Deliberately empty in the foundation phase (see dev request 1/5,
 * section 1: story content, interrogation branches, and puzzles are out
 * of scope here). Phases 2-5 populate evidence, suspects, dialogue trees,
 * and puzzle definitions in this folder — UI components should import
 * from here rather than hardcoding copy, per principle #4.
 */

export const CASE01_ID = "case01";
